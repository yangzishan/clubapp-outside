//定于得到得值
export const mp3Info = state => state.mp3Info;

export const playList = state => state.playList;

export const playState = state => state.playState;

export const activeIndex = state => state.activeIndex;

export const activeIndex2 = state => state.activeIndex2;

export const durationTime = state => state.durationTime;

export const percent = state => state.percent;

export const currentTime = state => state.currentTime;

export const courseInfo = state => state.courseInfo;

export const fullScreen = state => state.fullScreen;

export const songReady = state => state.songReady;
